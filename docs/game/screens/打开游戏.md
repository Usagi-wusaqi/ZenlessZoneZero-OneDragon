---
screen_name: 打开游戏
appears_in: [游戏启动]
last_updated: 2026-07-06
source_image: _1783242299811
---

# 打开游戏(登录页)

## 何时出现
启动序列(米哈游启动页 → 警告:游戏前详阅 → 绝区零标题页)之后出现的登录页。多个子态(同一 `screen_name=打开游戏`):
- **loading 态**(_1783242291962):账号已缓存,进入后自动「加载配置数据中」(进度 %),无进入按钮。
- **ready 态**(_1783242299811,本基线):加载完出现 `点击进入游戏` 按钮,点此进入游戏(自动登录)。
- **退出登录弹窗态**(_1783247876257):ready 态点右下「切换账号/退出登录」按钮后弹出的确认弹窗,选「清除/保留登录记录」→ 确定/取消。
- **账号确认态**(_1783247887600):退出登录弹窗选「保留登录记录」+ 确定 后出现(**条件:曾有登录且保留登录状态**);显手机号账号 + `进入游戏` + `登录其他账号`。
- **手动登录态**(未截图,**待补**):无缓存账号 / 点「登录其他账号」→ 账号密码输入 + 进入游戏。

## 状态流转
子态间切换(动作/条件 → 下一态):

| 当前态 | 动作 / 条件 | 下一态 |
|---|---|---|
| (启动,账号已缓存) | 自动 | loading 态 |
| loading 态 | 配置数据加载完 | ready 态 |
| ready 态 | 点 `点击进入游戏` | 进游戏(出本屏,自动登录) |
| ready 态 | 点 `切换账号`(=退出登录) | 退出登录弹窗态 |
| 退出登录弹窗态 | 点 `取消` | ready 态(返回) |
| 退出登录弹窗态 | 选「保留登录记录」+ `确定` | 账号确认态 |
| 退出登录弹窗态 | 选「清除登录记录」+ `确定` | 手动登录态?(**待确认**) |
| 账号确认态 | 点 `账号确认-进入游戏` | 进游戏(用保留账号) |
| 账号确认态 | 点 `登录其他账号` | ?(**待确认**) |

## 识别特征(稳定锚点)
- **适龄提示**(右上):`16+` / `CADPA` / `适龄提示`。
- **右边沿 4 个图标按钮**(竖排,@x≈1820,y≈738/824/910/997,各 r≈23):`资源检测` / `设置` / `游戏公告` / `切换账号`(=退出登录)。均为**无文字图形按钮**,OCR 抓不到,靠模板匹配(模板在 `assets/template/enter_game/`)。
- **`点击进入游戏` 按钮**(中下 @(892,873,137×27),ready 态)—— 主交互。
- **版本号**(右下)。

> `screen_info`「打开游戏」(`打开游戏.yml`)已建模,覆盖多登录路径 area:`点击进入游戏` / `切换账号` / `国服-账号密码进入游戏`(+`-新`)/ `国际服-点击登录` / `B服新-同意隐私政策`。ready 态命中多个 area(点击进入游戏 / 切换账号 / 国际服-点击登录 / B服新-同意隐私政策)但仍 `is_precise=False` —— 待分析为何没精准(可能缺某个精准标志或 area 集合判定问题)。

## 可交互元素
- **`点击进入游戏`**(ready 态主按钮,@(892,873)):点此进入游戏(自动登录路径)。
- **右边沿 4 个图标按钮**(竖排):`资源检测` / `设置` / `游戏公告` / `切换账号`(=退出登录)。
- **退出登录弹窗**(弹窗态):2 单选 `退出登录-清除登录记录` / `退出登录-保留登录记录`(默认选「保留」)+ `按钮-退出登录-取消` / `按钮-退出登录-确定`。选中状态(绿色实心/空心)**需颜色检测**,OCR 看不出。
- **账号确认态**:`账号确认-进入游戏` @(907,608)(用保留账号进游戏)+ 手机号右侧下拉触发器 @(1218,497)(**template**:`账号下拉-点击展开` ▽ / `账号下拉-点击收起` △ —— OCR 抓不稳这个小图标,改用模板;▽=收起态点开历史账号列表如 `158****01`/`1天前`,△=展开态点收起)+ `登录其他账号` @(889,704)(→ ?**待确认**)。手机号 / 列表项是**动态值**,勿当特征。
- 手动登录态(**待补**):账号密码输入 + 进入游戏。

## 识别快照(2026-07-06 核对)

**匹配画面**:`打开游戏` (is_precise=False)

**匹配 area**(ready 态,_1783242299811):
| area | 类型 | 文本 / template_id | conf | 位置 (x,y,w×h) |
|---|---|---|---|---|
| 点击进入游戏 | text | 点击进入游戏 | 0.999 | 892,873,137×27 |
| 资源检测 | template | resource_check | 1.0 | 1797,715,46×46 |
| 设置 | template | settings | 1.0 | 1797,801,46×46 |
| 游戏公告 | template | announcement | 1.0 | 1797,887,46×46 |
| 切换账号 | template | switch_account | 0.994 | 1798,974,46×46 |
| 国际服-点击登录 | text | 点击登录 | 0.999 | 892,873,137×27 |
| B服新-同意隐私政策 | text | 同意(残缺) | 0.560 | 912,650,157×51 |

**OCR 文本**(全量,ready 态,_1783242299811):
| 文本 | 位置 (x,y,w×h) | 备注 |
|---|---|---|
| `点击进入游戏` | 892,873,137×27 | 稳定(主按钮,识别锚点) |
| `16+` | 1771,75,70×48 | 适龄提示(稳定) |
| `CADPA` | 1776,127,59×18 | 适龄提示(稳定) |
| `适龄提示` | 1766,148,82×25 | 适龄提示(稳定) |
| `同包` | 912,650,157×51 | 误识(疑为「同意隐私政策」残缺) |
| `C` | 1811,814,20×18 | 噪声 |
| `0` | 154,915,104×98 | 图形误识 |
| `CNPRODWin3.0.0_R17093596_S170935` | 1558,1061,256×17 | 版本号(随 build 变) |

**loading 态**(_1783242291962):
| 文本 | 位置 (x,y,w×h) | 备注 |
|---|---|---|
| `加载配置数据中` | 880,874,159×26 | loading 状态文字(本态特有) |
| `12.27%` | 920,955,81×28 | 加载进度(易变) |
| `16+` / `CADPA` / `适龄提示` | 右上 | 同 ready 态 |
| `CNPRODWin3.0.0R17093596_S170935` | 1559,1062,255×14 | 版本号 |

**退出登录弹窗态**(_1783247876257):
- 匹配画面:`打开游戏` (is_precise=False) —— 命中弹窗 area + 背景 4 图标按钮(被蒙层压暗,conf 0.71~0.81)。另有通用「确定」误匹配 仓库/迷失之地/恢复电量(**噪声**,非相关)。

匹配 area:
| area | 类型 | 文本/template_id | conf | 位置 (x,y,w×h) |
|---|---|---|---|---|
| 标题-退出登录 | text | 退出登录 | 0.9998 | 587,297,119×35 |
| 退出登录-清除登录记录 | text | 退出并清除登录记录 | 0.986 | 662,432,215×28 |
| 退出登录-保留登录记录 | text | 退出并保留登录记录 | 0.969 | 662,510,215×27 |
| 按钮-退出登录-取消 | text | 取消 | 0.999 | 791,760,57×31 |
| 按钮-退出登录-确定 | text | 确定 | 0.999 | 1113,758,59×34 |
| 切换账号确定 | text | 确定 | 0.999 | 1113,758,59×34 |
| 切换账号 | template | switch_account | 0.738 | 1798,974,46×46 |
| 资源检测 | template | resource_check | 0.711 | 1797,715,46×46 |
| 设置 | template | settings | 0.814 | 1797,801,46×46 |
| 游戏公告 | template | announcement | 0.785 | 1797,887,46×46 |

OCR 文本(全量):
| 文本 | 位置 (x,y,w×h) | 备注 |
|---|---|---|
| `退出登录` | 587,297,119×35 | 标题(稳定) |
| `退出井清除登录记录` | 662,432,215×28 | 「并」误识为「井」 |
| `下次在该设备需要重新登录当前账号` | 663,469,323×22 | 清除项副文 |
| `退出井保留登录记录` | 662,510,215×27 | 「并」误识 |
| `下次在该设备支持自动登录当前账号，请确保设备可信` | 662,545,487×25 | 保留项副文(默认选中) |
| `取消` | 791,760,57×31 | 按钮 |
| `确定` | 1113,758,59×34 | 按钮 |
| `CNPRODWin3.0.0_R17093596_S170935` | 1558,1061,256×17 | 版本号 |

**账号确认态**(_1783247887600):
- 匹配画面:`打开游戏` (is_precise=False) —— 命中 `账号确认-进入游戏`(0.999)/ `按钮-登陆其他账号`(0.999)/ `账号下拉-点击展开`(▽,1.0)+ 右沿图标(切换账号/设置/游戏公告,conf 0.74~0.81)。右上关闭 X 被通用关闭模板命中 → 误匹配 `画面-通用`/`菜单`/`迷失之地-入口`/`影像店营业`(**噪声**)。
  - **下拉展开态变体**(_1783247896985):命中 `账号下拉-点击收起`(△,1.0)+ 多出历史账号列表(`135*****95`/刚刚、`158****01`/1天前,**动态**)。

匹配 area:
| area | 类型 | 文本/template_id | conf | 位置 (x,y,w×h) |
|---|---|---|---|---|
| 账号确认-进入游戏 | text | 进入游戏 | 0.999 | 907,608,107×35 |
| 按钮-登陆其他账号 | text | 登录其他账号 | 0.999 | 889,704,146×31 |
| 账号下拉-点击展开 | template | account_dropdown_expand | 1.0 | 1218,497,15×15 |
| 切换账号 | template | switch_account | 0.738 | 1798,974,46×46 |
| 设置 | template | settings | 0.814 | 1797,801,46×46 |
| 游戏公告 | template | announcement | 0.785 | 1797,887,46×46 |

OCR 文本(全量):
| 文本 | 位置 (x,y,w×h) | 备注 |
|---|---|---|
| `miHoYo` | 884,297,151×49 | logo(稳定) |
| `135*****95` | 733,485,151×39 | 手机号账号(**动态**,勿当特征) |
| `进入游戏` | 907,608,107×35 | 按钮(账号确认-进入游戏) |
| `登录其他账号` | 889,704,146×31 | 按钮 |
| `CNPRODWin3.0.0_R17093596_S170935` | 1558,1058,259×21 | 版本号 |

## 备注
- **右边沿图标按钮**:4 个无文字图标按钮(资源检测/设置/游戏公告/切换账号),OCR 看不见,用传统 CV(HoughCircles)定位 + 裁模板建 `screen_info` template area(模板目录 `assets/template/enter_game/`)。
- **退出登录弹窗**:弹窗态可交互元素已补 area(标题-退出登录 / 2 单选项 / 取消 / 确定);选中状态(哪个 radio 绿)OCR 看不出,需颜色阈值或选中/未选中 radio 模板(**待补**)。
- **`切换账号` ≡ 退出登录**:底部那个按钮(y≈997)既是「切换账号」也是「退出登录」(同义),screen_info 沿用既有 area_name「切换账号」,不改名。
- **两个子态**:本屏自动登录路径下有 loading → ready 两态(同一 `screen_name=打开游戏`);手动登录态未截(**待补**)。
- **版本**:3.0.0(CN/Windows)—— 校正:游戏当前为 3.0.0(早前 overview 草稿猜 2.x 偏低,重建时更正)。
- **动态值勿当特征**:加载进度 `%`、版本号随 build 变;识别用 `切换账号` / `点击进入游戏` + 适龄提示。
- **变更检测**:重新截图跑 `analyze_screen` 与上方快照 diff;重点看 按钮文字 / 适龄提示 是否在原位。
